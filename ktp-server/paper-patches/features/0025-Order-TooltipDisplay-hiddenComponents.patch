From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Bjarne Koll <git@lynxplay.dev>
Date: Sun, 22 Mar 2026 16:09:49 +0000
Subject: [PATCH] Order TooltipDisplay hiddenComponents

The usage of ReferenceLinkedOpenHashSet is rather questionable as it
maintains a specific iteration order while completely ignoring it for
comparison purposes.
As such, it breaks any form of consistent serialisation of itemstacks as
the encoder cache considers two ReferenceLinkedOpenHashSet instances
with different iteration orders equal even if they would not produce an
equal nbt output.

Replace it with the RegistrySinkSortedSet, which uses the registry id of
each data component to enforce order.

diff --git a/src/main/java/dev/lynxplay/ktp/item/component/DataComponentSequencedSet.java b/src/main/java/dev/lynxplay/ktp/item/component/DataComponentSequencedSet.java
new file mode 100644
index 0000000000000000000000000000000000000000..c91cb72a4a0e4b3a68ae2b78017621e0f52bb8ff
--- /dev/null
+++ b/src/main/java/dev/lynxplay/ktp/item/component/DataComponentSequencedSet.java
@@ -0,0 +1,19 @@
+package dev.lynxplay.ktp.item.component;
+
+import dev.lynxplay.ktp.core.RegistrySinkSequencedSet;
+import net.minecraft.core.component.DataComponentType;
+import net.minecraft.core.registries.BuiltInRegistries;
+
+import java.util.Collection;
+
+public class DataComponentSequencedSet extends RegistrySinkSequencedSet<DataComponentType<?>> {
+
+    public DataComponentSequencedSet() {
+        super(BuiltInRegistries.DATA_COMPONENT_TYPE);
+    }
+
+    public DataComponentSequencedSet(final Collection<? extends DataComponentType<?>> other) {
+        super(BuiltInRegistries.DATA_COMPONENT_TYPE, other);
+    }
+
+}
diff --git a/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaItem.java b/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaItem.java
index 68a020fc4637646e04c164379515994912fd6fd1..d81337bdc8f37231c48a5c5e3ad35b4d5e21f88e 100644
--- a/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaItem.java
+++ b/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaItem.java
@@ -385,7 +385,7 @@ public class CraftMetaItem implements ItemMeta, Damageable, Repairable, BlockDat
 
         this.repairCost = meta.repairCost;
         if (meta.tooltipDisplay != null) {
-            this.tooltipDisplay = new TooltipDisplay(meta.tooltipDisplay.hideTooltip(), new ReferenceLinkedOpenHashSet<>(meta.tooltipDisplay.hiddenComponents()));
+            this.tooltipDisplay = new TooltipDisplay(meta.tooltipDisplay.hideTooltip(), new dev.lynxplay.ktp.item.component.DataComponentSequencedSet(meta.tooltipDisplay.hiddenComponents()));  // KTP - order tooltip display hiddenComponents
         }
         this.tooltipStyle = meta.tooltipStyle;
         this.itemModel = meta.itemModel;
@@ -465,7 +465,7 @@ public class CraftMetaItem implements ItemMeta, Damageable, Repairable, BlockDat
         });
 
         getOrEmpty(patch, CraftMetaItem.TOOLTIP_DISPLAY).ifPresent((tooltipDisplay) -> {
-            this.tooltipDisplay = new TooltipDisplay(tooltipDisplay.hideTooltip(), new ReferenceLinkedOpenHashSet<>(tooltipDisplay.hiddenComponents()));
+            this.tooltipDisplay = new TooltipDisplay(tooltipDisplay.hideTooltip(), new dev.lynxplay.ktp.item.component.DataComponentSequencedSet(tooltipDisplay.hiddenComponents()));  // KTP - order tooltip display hiddenComponents
         });
 
         getOrEmpty(patch, CraftMetaItem.TOOLTIP_STYLE).ifPresent((tooltipStyle) -> {
@@ -975,7 +975,7 @@ public class CraftMetaItem implements ItemMeta, Damageable, Repairable, BlockDat
         }
 
         if (this.tooltipDisplay != null) {
-            tag.put(CraftMetaItem.TOOLTIP_DISPLAY, new TooltipDisplay(this.tooltipDisplay.hideTooltip(), new ReferenceLinkedOpenHashSet<>(this.tooltipDisplay.hiddenComponents())));
+            tag.put(CraftMetaItem.TOOLTIP_DISPLAY, new TooltipDisplay(this.tooltipDisplay.hideTooltip(), new dev.lynxplay.ktp.item.component.DataComponentSequencedSet(this.tooltipDisplay.hiddenComponents())));  // KTP - order tooltip display hiddenComponents
         }
 
         if (this.hasTooltipStyle()) {
@@ -1306,7 +1306,7 @@ public class CraftMetaItem implements ItemMeta, Damageable, Repairable, BlockDat
     @Override
     public void addItemFlags(ItemFlag... hideFlags) {
         if (this.tooltipDisplay == null) {
-            this.tooltipDisplay = new TooltipDisplay(false, new ReferenceLinkedOpenHashSet<>());
+            this.tooltipDisplay = new TooltipDisplay(false, new dev.lynxplay.ktp.item.component.DataComponentSequencedSet());  // KTP - order tooltip display hiddenComponents
         }
         for (ItemFlag f : hideFlags) {
             this.tooltipDisplay.hiddenComponents().addAll(ITEM_FLAG_EQUIVALENTS.get(f));
@@ -1484,7 +1484,7 @@ public class CraftMetaItem implements ItemMeta, Damageable, Repairable, BlockDat
     @Override
     public void setHideTooltip(boolean hideTooltip) {
         if (this.tooltipDisplay == null) {
-            this.tooltipDisplay = new TooltipDisplay(hideTooltip, new ReferenceLinkedOpenHashSet<>());
+            this.tooltipDisplay = new TooltipDisplay(hideTooltip, new dev.lynxplay.ktp.item.component.DataComponentSequencedSet());  // KTP - order tooltip display hiddenComponents
         } else {
             this.tooltipDisplay = new TooltipDisplay(hideTooltip, this.tooltipDisplay.hiddenComponents());
         }
@@ -2102,7 +2102,7 @@ public class CraftMetaItem implements ItemMeta, Damageable, Repairable, BlockDat
             clone.removedTags = Sets.newHashSet(this.removedTags);
             clone.persistentDataContainer = new CraftPersistentDataContainer(this.persistentDataContainer.getTagsCloned(), CraftMetaItem.DATA_TYPE_REGISTRY); // Paper - deep clone NBT tags
             if (this.tooltipDisplay != null) {
-                clone.tooltipDisplay = new TooltipDisplay(this.tooltipDisplay.hideTooltip(), new ReferenceLinkedOpenHashSet<>(this.tooltipDisplay.hiddenComponents()));
+                clone.tooltipDisplay = new TooltipDisplay(this.tooltipDisplay.hideTooltip(), new dev.lynxplay.ktp.item.component.DataComponentSequencedSet(this.tooltipDisplay.hiddenComponents()));  // KTP - order tooltip display hiddenComponents
             }
             clone.tooltipStyle = this.tooltipStyle;
             clone.itemModel = this.itemModel;
diff --git a/src/test/java/org/bukkit/craftbukkit/inventory/YamlSerializationTest.java b/src/test/java/org/bukkit/craftbukkit/inventory/YamlSerializationTest.java
index a525e4b10d667596f1823608ab11066554f8bae4..c729dc0fe8f174b98ec0f0a2f1647b2e36b06fc4 100644
--- a/src/test/java/org/bukkit/craftbukkit/inventory/YamlSerializationTest.java
+++ b/src/test/java/org/bukkit/craftbukkit/inventory/YamlSerializationTest.java
@@ -98,7 +98,7 @@ public class YamlSerializationTest {
               count: 1
               components:
                 minecraft:unbreakable: '{}'
-                minecraft:tooltip_display: '{hidden_components:["minecraft:enchantments","minecraft:unbreakable"]}'
+                minecraft:tooltip_display: '{hidden_components:["minecraft:unbreakable","minecraft:enchantments"]}' # KTP - sort hidden components set
                 minecraft:enchantments: '{"minecraft:sharpness":2}'
               schema_version: 1
             """.formatted(Bukkit.getUnsafe().getDataVersion()));
